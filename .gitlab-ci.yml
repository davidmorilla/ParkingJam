stages:          # List of stages for jobs, and their order of execution
  - test
  #- sonar

maven_build:
  stage: test
  image: maven:3.6.3-jdk-11
  script:
    - mvn test
  only:
    changes:
      - '**/*.java' # Ejecuta solo cuando se modifican archivos Java


#sonar_qube :
   # stage: sonar
   # mvn clean verify sonar : sonar - Dsonar . id = $XXX - Dsonar . login = $YYY